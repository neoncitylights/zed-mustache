name = "Mustache"
grammar = "mustache"
path_suffixes = ["mustache"]
block_comment = ["{{!", "}}"]
autoclose_before = "}"
brackets = [
	{ start = "<", end = ">", close = false, newline = true, not_in = ["comment"] },
    { start = "{", end = "}", close = true, newline = true }
]
